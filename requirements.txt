# ── Core (required for classifier + dashboard) ────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.20.0
dash>=2.18.0
dash-bootstrap-components>=1.5.0
pyarrow>=14.0.0          # parquet read/write

# ── Visualisation (regime history charts) ────────────────────────────────────
matplotlib>=3.7.0

# ── Data refresh only (scripts/update_data.py) ───────────────────────────────
# These are NOT required to run the classifier or dashboard once
# fred_raw.parquet exists.  Install only when refreshing data:
#   pip install fredapi requests
fredapi>=0.5.1
requests>=2.31.0
